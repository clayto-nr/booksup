// livros_page.dart
import 'package:flutter/material.dart';

class LivrosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Livros'),
      ),
      body: Center(
        child: Text('Página de Livros'),
      ),
    );
  }
}
